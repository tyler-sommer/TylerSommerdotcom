TylerSommer\Bundle\BlogBundle\Entity\Post:
  constraints:
    - Symfony\Bridge\Doctrine\Validator\Constraints\UniqueEntity: { fields: slug, message: 'Slug is already in use.' }

TylerSommer\Bundle\BlogBundle\Entity\Page:
  constraints:
    - Symfony\Bridge\Doctrine\Validator\Constraints\UniqueEntity: { fields: slug, message: 'Slug is already in use.' }
